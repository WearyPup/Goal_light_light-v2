# Render — Variables d'environnement
# À configurer dans Dashboard → Your Service → Environment

# ID équipe NHL (8 = Canadiens de Montréal)
TEAM_ID=8

# Port (Render le gère automatiquement via $PORT)
# PORT=3000
